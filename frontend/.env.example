# URL completa da API (recomendado em dev quando o backend não está na porta padrão)
# Ex: http://127.0.0.1:8080/api
VITE_API_URL=http://127.0.0.1:8080/api

# Alternativa: proxy do Vite usa esta URL quando VITE_API_URL não está definida
# VITE_API_PROXY_TARGET=http://127.0.0.1:8080
